{
  "name": "launcher",
  "description": "Manage macOS launch agents from the command line",
  "bin": { "launcher": "./bin/launcher" },
  "os": ["darwin"],
  "license": "MIT",
  "scripts": {
    "test": "PROJECT_ROOT=$PWD bats -r test"
  },
  "devDependencies": {
    "bats": "^1.11.0",
    "bats-support": "^0.3.0",
    "bats-assert": "^2.1.0"
  }
}
