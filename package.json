{
  "name": "launcher",
  "version": "0.1.0",
  "description": "Manage macOS launch agents from the command line",
  "bin": {
    "launcher": "./bin/launcher"
  },
  "scripts": {
    "test": "PROJECT_ROOT=$PWD bats --recursive test/"
  },
  "devDependencies": {
    "bats": "^1.10.0",
    "bats-support": "^0.3.0",
    "bats-assert": "^2.1.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mracos/launcher.git"
  },
  "license": "MIT",
  "os": ["darwin"]
}
